{
  "name": "hsf-story-studio-deploy",
  "version": "0.1.0",
  "private": true,
  "description": "Hoch Storybook Factory (HSF) — Story Studio deploy bundle: Stripe checkout + signed webhook + entitlement store. CommonJS (no \"type\":\"module\") to match the existing api/*.js functions.",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "stripe": "^16.12.0",
    "@vercel/kv": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
