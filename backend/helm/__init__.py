"""backend.helm — control-plane services (health registry, unified console wiring).

New package added by SWARM-1. Kept separate from backend/mission_control and
backend/jspace so it can be edited without contending on those files' leases.
"""
