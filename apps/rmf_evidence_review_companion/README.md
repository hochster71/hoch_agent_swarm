# RMF Evidence Review Companion — RC1

A standalone, privacy-first, offline compliance review companion for NIST SP 800-53 security control baselines.

## Setup & Build
1. Install Flutter SDK.
2. Run `flutter pub get`.
3. Build for iOS: `./scripts/build_ios_debug.sh`.
