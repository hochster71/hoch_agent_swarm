completion_gates:
  required_gates:
    - gate: "File diff"
      description: "Required for Code/docs changes"
    - gate: "Build output"
      description: "Required for Frontend/backend changes"
    - gate: "Unit tests"
      description: "Required for Logic/policy changes"
    - gate: "E2E tests"
      description: "Required for UI/control-plane changes"
    - gate: "Healthcheck"
      description: "Required for Runtime changes"
    - gate: "Security scan"
      description: "Required for Auth, execution, dependency, policy changes"
    - gate: "Backup"
      description: "Required for Risky state changes"
    - gate: "Evidence markdown"
      description: "Required for Milestone completion"
    - gate: "Clean git status"
      description: "Required for Baseline closure"
