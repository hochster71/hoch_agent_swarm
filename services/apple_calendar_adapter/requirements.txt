# Apple Calendar CalDAV adapter — runtime dependencies.
# Pinned to reasonable, compatible ranges. Install into an isolated venv.
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
caldav>=1.3,<2.0
pydantic>=2.7,<3.0
icalendar>=5.0,<7.0
