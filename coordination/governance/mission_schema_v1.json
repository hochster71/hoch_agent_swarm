{
  "schema": "HELM_MISSION_CONTRACT_v1",
  "doctrine": "Mission overlays change constantly; role doctrines rarely; provider bindings are replaceable. This is the overlay contract. SCOPE and TOOLS_ALLOWED are allowlists — anything not named is denied.",
  "adopted_by": "docs/helm/edr/EDR-0007-mission-contract-v1.md",
  "enforced_by": "backend/helm_runtime/mission_contract.py",
  "template": "docs/helm/mission/MISSION_TEMPLATE_v1.md",
  "required_fields": [
    "MISSION_ID", "TITLE", "OWNER", "ROLE", "OBJECTIVE", "SUCCESS_CRITERIA",
    "EXPECTED_OUTPUTS", "SCOPE", "TOOLS_ALLOWED", "EDR_REQUIRED",
    "FOUNDER_GATES", "STOP_CONDITIONS", "EVIDENCE_REQUIRED", "TRUTH_SOURCE", "RETURN"
  ],
  "optional_fields": ["INPUTS", "CONSTRAINTS", "PARENT_MISSION_ID", "CORRELATION_ID"],
  "list_fields": [
    "SUCCESS_CRITERIA", "INPUTS", "EXPECTED_OUTPUTS", "SCOPE", "TOOLS_ALLOWED",
    "CONSTRAINTS", "FOUNDER_GATES", "STOP_CONDITIONS", "EVIDENCE_REQUIRED"
  ],
  "enums": {
    "OWNER": ["founder", "orchestrator"],
    "ROLE": ["orchestrator", "builder", "auditor", "research", "security"],
    "EDR_REQUIRED": ["YES", "NO"],
    "TRUTH_SOURCE": [
      "LIVE_RUNTIME", "TEST_EXECUTION", "DETERMINISTIC_SCRIPT",
      "STATIC_ANALYSIS", "HUMAN_INPUT", "UNKNOWN"
    ],
    "FOUNDER_GATES": [
      "SPEND", "KEYS", "RELEASE", "SUBMISSION", "MONEY",
      "PRODUCTION_DEPLOY", "CREDENTIAL_CREATION", "EXTERNAL_IRREVERSIBLE"
    ],
    "RETURN_VALUES": ["DONE", "PARTIAL", "BLOCKED"]
  },
  "truth_source_projection": {
    "note": "TRUTH_SOURCE names the MECHANISM. It projects onto the founder-ratified truth classification in backend/security/proof_contract.py. It is never a second truth vocabulary. Only OBSERVED and DERIVED are in ADVANCING.",
    "LIVE_RUNTIME": "OBSERVED",
    "TEST_EXECUTION": "OBSERVED",
    "DETERMINISTIC_SCRIPT": "DERIVED",
    "STATIC_ANALYSIS": "ASSERTED",
    "HUMAN_INPUT": "ASSERTED",
    "UNKNOWN": "UNKNOWN"
  },
  "invariants": [
    "Every declared FOUNDER_GATE requires a stop condition mentioning the founder gate — declaring a gate never authorizes it.",
    "EDR_REQUIRED=YES obliges an EDR path in EXPECTED_OUTPUTS before the mission may return DONE.",
    "A mission may not return DONE on a non-advancing TRUTH_SOURCE (STATIC_ANALYSIS, HUMAN_INPUT, UNKNOWN).",
    "SCOPE and TOOLS_ALLOWED are allowlists; an empty allowlist denies everything and is a validation error, not a wildcard.",
    "Validation fails CLOSED: an unparseable or incomplete mission is BLOCKED, never best-effort."
  ]
}
