# Deprecated filename

Use `coordination/governance/role_overlays/ROLE_AUDITOR.md`.

Bindings: `coordination/governance/role_bindings.json`.

Roles are model-agnostic. Provider/model is bound at runtime.
