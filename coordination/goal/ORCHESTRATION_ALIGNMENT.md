# FOUNDER STANDING ORDER — Orchestration Alignment

> Authored by the Builder on Michael Hoch's explicit instruction, 2026-07-17.
> **Founder-owned.** Supersedes any role's self-initiated priority. Not in force
> until Michael ratifies (edit freely, then it commits to the Mission Runtime as
> a founder transaction). This document is the single alignment control object;
> all three frontiers obey it.

## 1. ALL-STOP — revenue push HALTED
No frontier works revenue (Stripe / Story Studio checkout / Epic Fury
monetization / first-dollar) until **HELM is operational** and the founder lifts
this halt in writing. Existing revenue evidence stays **frozen and untouched**.

## 2. One active phase
The only active phase is **HELM OPERATIONAL ALIGNMENT**. One mission, one queue,
founder-sequenced. No parallel initiatives.

## 3. The frontiers operate UNDER the founder
No model self-initiates work, edits shared files, mints verification targets,
changes a status, or commits — **except a task the founder has assigned** in the
active queue, routed through the Mission Runtime (the single door). If it isn't a
founder-directed task, it does not run.

## 4. Role lanes (reaffirmed, no crossing)
- **Founder (Michael)** — sets the active task queue and sequence; owns all gates.
- **Orchestrator** — plans and sequences *under the founder*; never approves,
  audits, or writes production code.
- **Builder (Claude)** — implements only assigned tasks; one at a time; EDR per
  architectural change.
- **Auditor (Grok)** — verifies only; never authors what it audits.

## 5. Anti-thrash rules
- **One writer at a time** via optimistic concurrency; no parallel edits to the
  same files.
- **No status upgrade without evidence** (no "verified" without the artifact).
- **No new architecture layers** until the current substrate is exercised.
- If two roles want the same file, the founder sequences them; they do not race.

## 6. How the founder drives it
Michael maintains the active queue (below). Frontiers pull the next assigned task,
do only that, report through the runtime, and stop. Nothing else executes.

### Active queue (founder sets this)
1. _(empty — Michael fills in the first aligned task)_

---
**Ratification:** Michael edits/approves this order; it then commits to the
Mission Runtime as a founder transaction and becomes the standing constraint every
frontier reads before acting.
